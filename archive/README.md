# Archive

Legacy files that have been superseded by the current project structure. Kept for reference — not used by active development.

## What's Here

### notes-legacy/
Sprint build plans, rally reports, founder notes, specs, and the mascot spec that originally lived in `/notes/`. These have been placed into their proper canonical locations:

- Sprint plans → `sprints/major/archive/` and `sprints/major/phase-1a/`
- Rally reports → `sprints/rallies/rally-001-full-audit/`
- Mascot spec → `docs/design-system/reference/`
- Technical specs → `sprints/rallies/rally-001-full-audit/specs/`

### ops-legacy/
The original `ops/` folder contents — an early execution log (`LOG.md`), todo list, and stack doc from before `CHANGELOG.md` took over as the cross-agent handover log.

## Note

The original files may still exist in `notes/` and `ops/`. This archive is a snapshot confirming they've been relocated. The canonical versions are in the locations listed above.
